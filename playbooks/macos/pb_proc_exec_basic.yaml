playbook_id: pb_proc_exec_basic
title: Process Execution Baseline
severity: low
entity_scope: host|user|exe
tags:
  - baseline
  - process_execution
description: Detects any process execution (baseline for all other playbooks)

slots:
  - slot_id: proc_exec
    required: true
    ttl_seconds: 300
    event_type:
      - proc_exec

fire:
  required_slots:
    - proc_exec
  narrative: Process execution detected
